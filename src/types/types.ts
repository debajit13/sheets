export interface Change {
  x: number;
  y: number;
  value: string;
}

export interface VisibleResult {
  visible: number[];
  start: number[];
  end: number[];
}

export interface CoordinateType {
  x: number;
  y: number;
}
